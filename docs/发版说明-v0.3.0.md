# 发版说明 v0.3.0 — macOS 通知链路回归

> 发版日期：2026-04-06

## 概述

v0.3.0 把 LongRun 中已经验证过的 macOS 系统通知能力，按 iLongRun 当前架构完整补回。

这一版的目标很明确：**让 iLongRun 在后台长跑、自动恢复、完成收尾和需要人工介入时，都能主动把状态提醒送到 Mac 用户面前。**

## 这次改了什么

### 完整通知链路回归

现在 iLongRun 支持以下通知事件：
- `launched`
- `resumed`
- `recovery`
- `attention`
- `complete`
- `blocked`
- `checkpoint`

其中：
- detached 启动成功后会提醒
- resume 成功后会提醒
- 首次自动恢复 / fallback 时会提醒
- complete precheck 失败时会提醒
- 最终完成 / 阻塞时会提醒

### 通知后端：增强优先，基础保底

通知后端按以下顺序工作：
1. `terminal-notifier`
2. `osascript`
3. 非 macOS 或 dry-run 时输出模拟结果

这意味着：
- 装了 `terminal-notifier` 时，会优先获得更完整的通知体验
- 没装时，macOS 仍然可以通过系统自带 `osascript` 收到基础提醒

### 点击通知后的打开目标

这次**没有照搬 LongRun 的独立“启动看板”文件**，而是直接复用 iLongRun 已有产物：
- `launched` / `resumed` / `recovery` / `attention`：优先打开 `plan.md`
- `complete` / `blocked`：优先打开 `COMPLETION.md`

这样可以减少额外文件协议，更贴合 iLongRun 当前结构。

### doctor 自检命令

现在可以直接用下面命令检查提醒链路：

```bash
ilongrun-doctor --notify-test
```

如果没有看到提醒横幅，请优先检查：
- 系统设置 > 通知
- 专注模式
- 通知摘要

## 涉及文件

- `scripts/notify_macos.py`
- `scripts/copilot-ilongrun`
- `scripts/launch_ilongrun_supervisor.py`
- `scripts/finalize_ilongrun_run.py`
- `scripts/install-bare-commands.sh`
- `scripts/uninstall-bare-commands.sh`
- `scripts/selftest_ilongrun.py`
- `README.md`
- `docs/快速开始.md`
- `CHANGELOG.md`
- `plugin.json`

## 安装 / 升级

```bash
curl -fsSL https://raw.githubusercontent.com/izscc/iLongRun/main/install.sh | bash
```

安装后建议执行：

```bash
ilongrun-doctor --refresh-model-cache
ilongrun-doctor --notify-test
```

## 兼容性

- 不改变 iLongRun 的 run 目录协议
- 不改变 `scheduler.json` 结构
- 不改变 `ilongrun / ilongrun-resume / ilongrun-status` 命令入口
- 与 v0.2.1 保持兼容

## 说明

- 可通过 `ILONGRUN_NOTIFICATIONS=0` 关闭通知
- 卸载 helper 时不会自动删除第三方 `terminal-notifier` 安装物，这与 LongRun 行为保持一致
