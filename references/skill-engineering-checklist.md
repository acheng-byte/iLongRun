# Skill Engineering Checklist

- frontmatter `description` 是否是 trigger-first 触发句（如 `Use when...` / `当用户...时使用` / `当需要...时使用`）
- 顶层 skill 是否只保留触发条件与路由职责
- 是否把复杂流程拆进 playbook
- 是否存在必要章节
- 是否有清晰 cross-reference
- 是否避免把实现细节塞进 description
- 是否为高频 skill 控制了词数预算
- 是否存在最少一组 pressure scenario
